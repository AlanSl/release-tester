name: release

on:
  workflow_dispatch:
    inputs:
      semver:
        description: The semver to use
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
  pull_request:
    types: [closed]

jobs:
  release:
    permissions:
      pull-requests: write
      contents: write
    runs-on: ubuntu-latest
    steps:
#     SHA of latest commit in feat/provenance branch
      - uses: AlanSl/optic-release-automation-action@db0faf0c548d9aa829fa5ccce7782cf4d42c38a3
        with:
          semver: ${{ github.event.inputs.semver }}
          commit-message: 'chore: release {version}'
          npm-token: ${{ secrets[format('NPM_TOKEN_{0}', github.actor)] || secrets.NPM_TOKEN }}
          optic-token: ${{ secrets[format('OPTIC_TOKEN_{0}', github.actor)] || secrets.OPTIC_TOKEN }}
          build-command: npm ci
          provenance: true

